version: "3.2"

#services:
#  alpinewhatever:
#    image: "alpine:3.15"
#    env_file:
#      - ./.env

networks:
  regnonet:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: ${REGNONET_SUBNET}
  dmznet:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: ${DMZNET_SUBNET}

volumes:
  data-monerod:
  data-p2pool:
